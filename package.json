{
  "private": true,
  "scripts": {
    "dev": "npm --prefix server run dev",
    "start": "npm --prefix server start"
  },
  "dependencies": {
    "cloudinary": "^2.11.0",
    "multer": "^2.3.0"
  }
}
